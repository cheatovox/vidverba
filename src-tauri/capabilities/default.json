{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for VidVerba desktop windows.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}

